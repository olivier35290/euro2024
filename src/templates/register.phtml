<section>
    <!-- Affichage d'un message si la variable $message est définie -->
    <?php if (isset($message)): ?>
        <p><?= htmlspecialchars($message) ?></p>
    <?php endif; ?>

    <!-- Formulaire d'inscription -->
    <form method="POST" action="index.php?route=register" class="register-form">
        <!-- Champ pour le nom d'utilisateur -->
        <div class="form-group">
            <label for="username">Login</label>
            <input type="text" id="username" name="username" required>
            <!-- Message d'erreur pour le nom d'utilisateur déjà pris -->
            <span id="username-error" style="color: red; display: none;">Login déjà pris.</span>
        </div>
        
        <!-- Champ pour l'email -->
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required>
        </div>
        
        <!-- Champ pour confirmer l'email -->
        <div class="form-group">
            <label for="confirm_email">Confirmez l'email</label>
            <input type="email" id="confirm_email" name="confirm_email" required>
        </div>
        
        <!-- Champ pour le mot de passe -->
        <div class="form-group">
            <label for="password">Mot de passe</label>
            <input type="password" id="password" name="password" required>
        </div>
        
        <!-- Champ pour confirmer le mot de passe -->
        <div class="form-group">
            <label for="confirm_password">Confirmez le mot de passe</label>
            <input type="password" id="confirm_password" name="confirm_password" required>
        </div>
        
        <!-- Bouton de soumission du formulaire -->
        <div class="form-group">
            <button type="submit">S'inscrire</button>
        </div>
    </form>

    <!-- Affichage d'un message d'erreur si la variable $error est définie -->
    <?php if (isset($error)): ?>
        <p style="color: red;"><?= htmlspecialchars($error) ?></p>
    <?php endif; ?>
</section>
